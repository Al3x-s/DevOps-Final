import sqlite3
def printall():
    print("========================================")
    conn = sqlite3.connect("user.db")
    cursor = conn.cursor()
    cursor.execute("SELECT * FROM users")
    rows = cursor.fetchall()
    for i in rows:
        print(i)
    cursor.close()
    conn.close
printall()
